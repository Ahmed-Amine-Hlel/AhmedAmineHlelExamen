spring:
  application:
    name: formateur-service
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_formateurs
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 6005
